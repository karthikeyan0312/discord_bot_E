version: '3'

services:
  discord-bot:
    build: .
    container_name: discord-bot-einstein
    ports:
      - "8502:8502"
    dns:
      - 8.8.8.8
      - 8.8.4.4
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}  # Explicitly pass the variable
    restart: unless-stopped
